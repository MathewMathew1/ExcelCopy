import { api } from "~/trpc/react";

const Sheet = () => {

    //const { data: workbook } = api.workbook.getById.useQuery({ id: 123 });
    //console.log(workbook)

    return <div>
        Dazdingo
    </div>
}
 
export default Sheet;